# ออกแบบแอพบันทึกของที่ซื้อ + หารค่าใช้จ่าย 2 คน (Shop)

## 1. โจทย์

- มี 2 คน ซื้อของใช้ร่วมกัน ใครสะดวกคนนั้นจ่ายไปก่อน
- สิ้นเดือนเอายอดมารวมกันแล้วหารครึ่ง เพื่อดูว่าใครต้องโอนให้ใครเท่าไหร่
- ซื้อของจาก 2 ร้านหลัก
  - **ร้าน A มีบิล** → มีหลักฐานยอดชัดเจน ถ่ายรูปเก็บได้
  - **ร้าน B ไม่มีบิล** → ต้องจดเอง ถ้าไม่จดทันทีจะลืม

## 2. หลักการออกแบบ (สำคัญที่สุด)

1. **บันทึกให้เร็วที่สุด ณ จุดซื้อ** — โดยเฉพาะร้านที่ไม่มีบิล ถ้ากรอกช้าเกิน 10 วินาทีคนจะเลิกจด
   ดังนั้นหน้าเพิ่มรายการต้องกรอกแค่ 3 อย่างก็พอ: *ร้าน / ยอดเงิน / ใครจ่าย*
2. **รายละเอียดใส่ทีหลังได้** — รายการสินค้าย่อย, รูปบิล, หมายเหตุ เป็น optional ทั้งหมด
3. **บิลคือ "หลักฐานแนบ" ไม่ใช่ข้อบังคับ** — โครงสร้างข้อมูลของ 2 ร้านเหมือนกันทุกอย่าง
   ต่างกันแค่ร้าน A มีรูปบิลแนบ ร้าน B ไม่มี (หรือแนบรูปของที่ซื้อแทนก็ได้)
4. **สิ้นเดือนต้องกดปุ่มเดียวแล้วรู้เลยว่าใครโอนให้ใครเท่าไหร่**

## 3. โครงสร้างข้อมูล (Data Model)

```
Person   (คน)          : id, name
Store    (ร้าน)         : id, name, has_receipt (true/false)
Purchase (การซื้อ 1 ครั้ง): id, date, store_id, payer_id (ใครจ่าย),
                          total_amount, receipt_photo (optional),
                          note (optional), settled_month (null = ยังไม่เคลียร์)
Item     (ของในบิล, optional): id, purchase_id, name, price,
                          split_type: "shared" | "personal_of:<person_id>"
Settlement (การเคลียร์ยอด): id, month, from_person, to_person, amount, settled_at
```

จุดสำคัญ:

- **Purchase คือหน่วยหลัก** — 1 ครั้งที่จ่ายเงิน = 1 record ไม่ว่ามีบิลหรือไม่
- **Item เป็น optional** — ร้านมีบิลอาจอยากแยกรายการ (เผื่อบางชิ้นเป็นของส่วนตัวไม่หาร)
  ร้านไม่มีบิลใส่แค่ยอดรวมพอ
- ถ้าไม่แยก Item → ทั้ง Purchase ถือว่า "หารครึ่ง"
- ถ้าแยก Item → ชิ้นที่เป็น `personal_of` คิดเต็มให้เจ้าของ ที่เหลือหารครึ่ง

## 4. สูตรคำนวณสิ้นเดือน

สำหรับเดือนที่เลือก (เฉพาะ Purchase ที่ยังไม่ settle):

```
ยอดที่ A ควรจ่ายจริง (share_A) = ของส่วนตัว A + (ของที่หารกัน ÷ 2)
ยอดที่ A จ่ายไปแล้ว  (paid_A)  = รวม total_amount ของ Purchase ที่ payer = A

net_A = paid_A - share_A
  ถ้า net_A > 0 → B ต้องโอนให้ A เท่ากับ net_A
  ถ้า net_A < 0 → A ต้องโอนให้ B เท่ากับ |net_A|
```

กด "เคลียร์ยอดเดือนนี้" → สร้าง Settlement record + stamp `settled_month` ให้ทุก Purchase
ของเก่าจะไม่ถูกนับซ้ำในเดือนถัดไป และรายการที่กรอกย้อนหลังหลังเคลียร์แล้วจะไปโผล่รอบหน้าแทน

## 5. หน้าจอ (4 หน้าพอ)

### 5.1 หน้าเพิ่มรายการ (หน้าแรกของแอพ — Quick Add)
- ปุ่มร้านเป็น toggle ใหญ่ ๆ 2 ปุ่ม: [ร้าน A] [ร้าน B] (จำร้านล่าสุดไว้)
- ช่องยอดเงิน (numeric keypad เด้งทันที)
- toggle ใครจ่าย: [ผม] [อีกคน]
- ปุ่ม 📷 ถ่ายรูปบิล (โชว์เด่นเมื่อเลือกร้านที่ `has_receipt = true`)
- ปุ่ม "แยกรายการ" (optional, ไว้ระบุของส่วนตัวที่ไม่หาร)
- กดบันทึก → เสร็จใน 5–10 วินาที

### 5.2 หน้ารายการเดือนนี้
- list เรียงตามวัน โชว์ ร้าน / ยอด / ใครจ่าย / มีรูปบิลไหม (ไอคอน 🧾)
- แถบสรุปบนสุด: เดือนนี้จ่ายรวมเท่าไหร่ + ตอนนี้ใครแบกอยู่เท่าไหร่ (net แบบ real-time)
- แตะเพื่อแก้ไข/ลบ/เพิ่มรูปย้อนหลัง

### 5.3 หน้าเคลียร์ยอด (Settlement)
- สรุป: paid_A, paid_B, share_A, share_B, และบรรทัดใหญ่ **"B โอนให้ A 743 บาท"**
- ปุ่ม "เคลียร์แล้ว ✓" → บันทึก Settlement, ล็อกรายการของเดือนนั้น
- ประวัติการเคลียร์ย้อนหลัง

### 5.4 หน้าตั้งค่า
- ชื่อ 2 คน, จัดการร้าน (เพิ่ม/แก้ร้าน, ติ๊กว่ามีบิลไหม), สัดส่วนหาร (default 50/50 แต่เปลี่ยนได้)

## 6. รับมือร้านที่ "ไม่มีบิล" โดยเฉพาะ

- Quick Add ต้องเร็วพอที่จะกรอกหน้าร้านก่อนเดินออก — นี่คือทางแก้หลัก
- แนบ "รูปของที่ซื้อ" แทนบิลได้ เผื่ออยากมีหลักฐานกันลืม/กันงง
- มีช่อง note สั้น ๆ เช่น "ผัก+ไข่+หมู"
- เตือนความจำ (optional): notification ตอนเย็นถามว่า "วันนี้ซื้อของหรือเปล่า?" กันลืมจด
- ยอดผิดแก้ย้อนหลังได้เสมอ (ตราบใดที่เดือนนั้นยังไม่กดเคลียร์)

## 7. Tech Stack ที่แนะนำ

ใช้กันแค่ 2 คน → ไม่ต้องทำระบบใหญ่:

- **PWA (เว็บแอพติดหน้าจอมือถือ)**: Next.js/React + Tailwind
- **ฐานข้อมูล**: Supabase (Postgres + Auth + Storage เก็บรูปบิล) — ฟรีเทียร์เหลือเฟือ
  ทั้ง 2 คน login แล้วเห็นข้อมูลชุดเดียวกัน sync กันอัตโนมัติ
- ทางเลือกเบากว่า: LIFF (LINE Mini App) เพราะทั้งคู่มี LINE อยู่แล้ว ไม่ต้องติดตั้งอะไรเพิ่ม

## 8. ลำดับการพัฒนา (Roadmap)

1. ~~**MVP**: Quick Add (ร้าน/ยอด/ใครจ่าย) + รายการเดือนนี้ + หน้าเคลียร์ยอดหารครึ่ง~~ ✅
2. ~~**เฟส 2**: แนบรูปบิล, แยกของส่วนตัวไม่หาร, แก้ไข/ลบรายการ~~ ✅
   (ของส่วนตัวเก็บเป็นยอดต่อคนในบิล `personal_p1/p2_satang` — เบากว่าแยก Item รายชิ้น
   แต่ครอบคลุมโจทย์เดียวกัน: ส่วนไหนไม่หารคิดเต็มให้เจ้าของ)
3. **เฟส 3**: ~~ประวัติย้อนหลัง~~ ✅, ~~export CSV~~ ✅,
   ~~สแกนบิลอ่านยอดอัตโนมัติ (OCR)~~ ✅ (tesseract.js heb+eng ในเครื่อง,
   asset self-host ใน `public/ocr` — บิลอิสราเอล หายอดจากบรรทัด סה"כ/לתשלום),
   notification เตือนจด (ต้องมี push infra)

หมายเหตุ: แอพใช้สกุลเงินเชเขล (₪) และเขตเวลา Asia/Jerusalem —
คอลัมน์เงินยังชื่อ `*_satang` ตามเดิม (หน่วยเก็บ = 1/100 ₪ หรือ 1 agora)
